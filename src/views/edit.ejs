<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <title><%= title %></title>
</head>
<body>
    <div class="topnav">
        <a class="active" href="/">Home</a>
        <a href="/upload">Upload file</a>

      </div>
    <div>
        <div class="form__add--user"  >
            <form method="POST" action="/update">
              <h2>Edit user</h2>
              <label for="firstName">First Name</label>
              <input type="text" id="fname" name="firstName" value="<%= dataUserById.firstName %>" placeholder="Your name..">
          
              <label for="lastName">Last Name</label>
              <input type="text" id="lname" name="lastName" value="<%= dataUserById.lastName %>" placeholder="Your last name..">
          
              <label for="email">Your Email</label>
              <input type="text" id="email" name="email" value="<%= dataUserById.email %>" placeholder="Your Email..">
            
              <label for="phone">Your Phone</label>
              <input type="text" id="phone" name="phone" value="<%= dataUserById.phone %>" placeholder="Your Phone..">
                <input type="hidden" name="userId" value="<%= dataUserById.id %>">
              <input type="submit" value="update">
            </form>
          </div>
        </div>
    <style>
                    * {
            margin: 0;
            padding: 0;
        }

        /* Add a black background color to the top navigation */
        .topnav {
            background-color: #333;
            overflow: hidden;
        }
        
        /* Style the links inside the navigation bar */
        .topnav a {
        float: left;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
        font-size: 17px;
        }

        /* Change the color of links on hover */
        .topnav a:hover {
        background-color: #ddd;
        color: black;
        }

        /* Add a color to the active/current link */
        .topnav a.active {
        background-color: #04AA6D;
        color: white;
        }
    </style>
</body>
</html>